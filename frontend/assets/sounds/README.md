# Sounds (optional)

Drop `.mp3` / `.ogg` files here and wire them in `app.js` using the `audio` object.

Suggested files:
- `fly.mp3`   — looping engine hum during flight
- `crash.mp3` — explosion / crash SFX
- `tick.mp3`  — countdown tick
